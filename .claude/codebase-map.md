# Codebase Map
> Auto-generated by /map — 2026-04-01
> Updated: added goal, memory, dream, send, helpers, migrate-memory, migrate-mailbox commands

## Stack
- Language: Go 1.26
- Framework: Cobra (CLI) + Viper (config)
- Package manager: Go modules
- Release: GoReleaser → GitHub Actions on tag push

## Directory Structure
```
cubit/
├── main.go                  # Entry point → cmd.Execute()
├── go.mod / go.sum          # Module: github.com/SeanoChang/cubit
├── .goreleaser.yml          # Release config (linux/amd64 + darwin/arm64)
├── CLAUDE.md                # Project instructions
├── .github/workflows/
│   └── release.yml          # CI: GoReleaser on tag push
├── cmd/                     # Cobra command definitions
│   ├── root.go              # Root command, init() wires all subcommands
│   ├── helpers.go           # Shared utils: isValidAgentName()
│   ├── version.go           # `cubit version` — prints version/commit/date
│   ├── update.go            # `cubit update` — self-update from GitHub releases
│   ├── init.go              # `cubit init` — scaffold agent workspace
│   ├── status.go            # `cubit status` — goals, memory tokens, projects, log tail
│   ├── edit.go              # `cubit edit` — open agent files in $EDITOR
│   ├── archive.go           # `cubit archive` — log + scratch → nark, truncate log, clean scratch
│   ├── project.go           # `cubit project` — new/list/search/archive/status subcommands
│   ├── goal.go              # `cubit goal` — add/ls typed goals (session/background/consolidation)
│   ├── memory.go            # `cubit memory` — ls/check memory topic files and stats
│   ├── dream.go             # `cubit dream` — LLM-powered memory consolidation via claude CLI
│   ├── send.go              # `cubit send` — deliver mailbox messages between agents
│   ├── migrate.go           # `cubit migrate` — v0.x/flat → agents-home migration
│   ├── migrate_projects.go  # `cubit migrate-projects` — git-at-root → projects/ migration
│   ├── migrate_memory.go    # `cubit migrate-memory` — create memory/ dir for agents
│   └── migrate_mailbox.go   # `cubit migrate-mailbox` — create mailbox/ tree, migrate INBOX.md
├── internal/
│   ├── config/
│   │   ├── config.go        # Config struct, Load(), Default(), AgentDir(), IsLegacyLayout()
│   │   └── config_test.go   # Tests for config
│   ├── scaffold/
│   │   ├── scaffold.go      # Init() — creates agent workspace with .claude/, templates, projects/
│   │   └── scaffold_test.go # Tests for scaffold
│   ├── project/
│   │   └── project.go       # New(), List(), Search(), Archive(), Status() — project CRUD + git ops
│   └── updater/
│       ├── updater.go       # Update() — self-update via GitHub releases API
│       └── updater_test.go  # Tests for updater
└── docs/
    ├── v0.1–v0.4/           # Historical version READMEs
    └── superpowers/plans/   # Implementation plans
```

## Entry Points
| File | Purpose |
|------|---------|
| `main.go` | CLI entry → `cmd.Execute()` |
| `cmd/root.go` | Root Cobra command, all subcommand wiring in `init()` |

## Key Modules
### `cmd/`
| File | Exports / Responsibility |
|------|--------------------------|
| `root.go` | Root command, `--agent` flag, CWD auto-detection, legacy layout warnings |
| `helpers.go` | `isValidAgentName()` — path-safe agent name validation |
| `init.go` | `cubit init <agent>` — scaffold workspace with `--force`, `--import-identity` |
| `status.go` | `cubit status` — goals, memory tokens, projects, log tail |
| `edit.go` | `cubit edit <target>` — open agent files in `$EDITOR` |
| `archive.go` | `cubit archive` — push log+scratch to nark, `--keep-log` flag |
| `project.go` | `cubit project` — new/list/search/archive/status subcommands |
| `goal.go` | `cubit goal add/ls` — typed goals with session/background/consolidation types |
| `memory.go` | `cubit memory ls/check` — list topic files, show size stats |
| `dream.go` | `cubit dream` — invokes `claude -p` for memory consolidation, archives old MEMORY.md |
| `send.go` | `cubit send <draft>` — parse frontmatter, deliver to target agent's mailbox/inbox/ |
| `migrate.go` | `cubit migrate` — v0.x/flat → agents-home layout migration |
| `migrate_projects.go` | `cubit migrate-projects` — git-at-root → projects/ layout |
| `migrate_memory.go` | `cubit migrate-memory` — create memory/ directory |
| `migrate_mailbox.go` | `cubit migrate-mailbox` — create mailbox/ tree, migrate INBOX.md content |

### `internal/config`
| File | Exports / Responsibility |
|------|--------------------------|
| `config.go` | `Config`, `Load()`, `Default()`, `AgentDir()`, `IsLegacyLayout()`, `IsFlatLayout()`, `LegacyAgentDir()`, `FlatAgentDir()` |

### `internal/scaffold`
| File | Exports / Responsibility |
|------|--------------------------|
| `scaffold.go` | `Init(root, agent, force)` — creates agent dir with .claude/settings.json, .claude/agents/<agent>.md, template files (FLUCTLIGHT, PROGRAM, GOALS, MEMORY, log), scratch/, projects/, config.yaml |

### `internal/project`
| File | Exports / Responsibility |
|------|--------------------------|
| `project.go` | `New()`, `List()`, `Search()`, `Archive()`, `Status()`, `FormatAge()` — project CRUD, git-based search across projects, nark archival |

### `internal/updater`
| File | Exports / Responsibility |
|------|--------------------------|
| `updater.go` | `Update()` — checks GitHub releases API, downloads + replaces binary |

## Dependency Graph
```
main.go → cmd/
  cmd/root.go → internal/config
  cmd/init.go → internal/scaffold
  cmd/migrate.go → internal/scaffold
  cmd/migrate_projects.go → (os/exec for git)
  cmd/migrate_memory.go → (os only)
  cmd/migrate_mailbox.go → (os only)
  cmd/project.go → internal/project
  cmd/status.go → internal/project
  cmd/update.go → internal/updater
  cmd/edit.go → (os/exec only)
  cmd/archive.go → (os/exec for nark)
  cmd/goal.go → (os only — direct file I/O)
  cmd/memory.go → (os only — file walking + stats)
  cmd/dream.go → (os/exec for claude CLI)
  cmd/send.go → cmd/helpers (isValidAgentName)

  internal/scaffold → internal/config
```
