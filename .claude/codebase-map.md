# Codebase Map
> Auto-generated by /map — 2026-03-10

## Stack
- Language: Go 1.26
- Framework: Cobra (CLI) + Viper (config)
- Package manager: Go modules
- Release: GoReleaser → GitHub Actions on tag push

## Directory Structure
```
cubit/
├── main.go                  # Entry point → cmd.Execute()
├── go.mod / go.sum          # Module: github.com/SeanoChang/cubit
├── .goreleaser.yml          # Release config (linux/amd64 + darwin/arm64)
├── CLAUDE.md                # Project instructions
├── .github/workflows/
│   └── release.yml          # CI: GoReleaser on tag push
├── cmd/                     # Cobra command definitions
│   ├── root.go              # Root command, init() wires all subcommands + flags
│   ├── version.go           # `cubit version` — prints version/commit/date
│   ├── init.go              # `cubit init` — scaffold + onboarding
│   ├── config.go            # `cubit config show` — dump config
│   ├── todo.go              # `cubit todo` — create task
│   ├── queue.go             # `cubit queue` — list pending tasks
│   ├── do.go                # `cubit do` — pop ready task(s)
│   ├── done.go              # `cubit done` — complete active task
│   ├── requeue.go           # `cubit requeue` — return active → pending
│   ├── log.go               # `cubit log` — append observation to log.md
│   ├── prompt.go            # `cubit prompt` — single-shot Claude prompt with brief
│   ├── brief.go             # `cubit brief` — display session brief sections
│   ├── run.go               # `cubit run` — DAG executor (fan-out/fan-in, loop, retry)
│   ├── status.go            # `cubit status` — agent status overview
│   ├── refresh.go           # `cubit refresh` — rebuild brief.md from journals
│   ├── graph.go             # `cubit graph` — render task DAG (Mermaid/ASCII)
│   ├── update.go            # `cubit update` — self-update from GitHub releases
│   ├── identity.go          # `cubit identity` — list/show/set identity files
│   ├── summarize.go         # Helper: first-line summarizer for task output
│   └── summarize_test.go    # Tests for summarize
├── internal/
│   ├── config/
│   │   └── config.go        # Config struct, Load() via Viper, Default(), RunnerOpts()
│   ├── claude/
│   │   ├── runner.go        # Prompt() wraps `claude -p` via stdin, RunnerOpts
│   │   └── runner_test.go   # Tests for runner
│   ├── scaffold/
│   │   ├── scaffold.go      # Init() creates agent dir tree + default config
│   │   └── setup.go         # RunSetup() interactive LLM onboarding chat
│   ├── queue/
│   │   ├── task.go           # Task struct, ParseTask(), Serialize(), Slugify()
│   │   ├── task_test.go
│   │   ├── queue.go          # Queue singleton: Create/List/Pop/Complete/Requeue/Log
│   │   ├── queue_test.go
│   │   ├── graph.go          # DAG: BuildGraph, Subgraph, RenderMermaid/ASCII, DetectCycle
│   │   ├── graph_test.go
│   │   ├── executor.go       # TaskResult, WriteTaskOutput, DeadlockError
│   │   ├── executor_test.go
│   │   ├── iteration.go      # Get/Increment/ClearIteration for loop tasks
│   │   ├── iteration_test.go
│   │   ├── goal.go           # GoalMet() checks for GOAL_MET signal
│   │   ├── goal_test.go
│   │   ├── results.go        # ReadResults/AppendResult for memory/results.tsv
│   │   └── results_test.go
│   ├── brief/
│   │   ├── brief.go          # Build() assembles session brief, BuildWithUpstream, BuildLoopInjection
│   │   ├── brief_test.go
│   │   ├── memory.go         # RunMemoryPass() rewrites brief.md, RunRefresh() from journals
│   │   └── memory_test.go
│   └── updater/
│       ├── updater.go        # Update() self-update via GitHub releases API
│       └── updater_test.go
```

## Entry Points
| File | Purpose |
|------|---------|
| `main.go` | CLI entry → `cmd.Execute()` |
| `cmd/root.go` | Root Cobra command, all subcommand wiring in `init()` |

## Key Modules
### `internal/config`
| File | Exports / Responsibility |
|------|--------------------------|
| `config.go` | `Config`, `ClaudeConfig`, `Load()`, `Default()`, `AgentDir()`, `RunnerOpts()` |

### `internal/claude`
| File | Exports / Responsibility |
|------|--------------------------|
| `runner.go` | `RunnerOpts`, `Prompt()` — wraps `claude -p` CLI with stdin, model, permission-mode, timeout |

### `internal/scaffold`
| File | Exports / Responsibility |
|------|--------------------------|
| `scaffold.go` | `Init()` — creates agent directory tree (identity/, queue/, scratch/, memory/) |
| `setup.go` | `RunSetup()` — interactive onboarding: multi-round chat → generates FLUCTLIGHT.md + USER.md |

### `internal/queue`
| File | Exports / Responsibility |
|------|--------------------------|
| `task.go` | `Task` struct with YAML frontmatter, `ParseTask()`, `Serialize()`, `Slugify()` |
| `queue.go` | `Queue` singleton: `Create`, `List`, `Pop`, `PopReady`, `PopAllReady`, `Active`, `Complete`, `Requeue`, `Log`, `ValidateDependencies`, `NextID` |
| `graph.go` | `BuildGraph`, `Subgraph`, `RenderMermaid`, `RenderASCII`, `ReadyNodes`, `GraphComplete`, `DetectCycle` |
| `executor.go` | `TaskResult`, `WriteTaskOutput`, `DeadlockError` |
| `iteration.go` | `GetIteration`, `IncrementIteration`, `ClearIteration` — loop task state |
| `goal.go` | `GoalMet()` — checks output for GOAL_MET signal |
| `results.go` | `ReadResults`, `AppendResult` — TSV logging for loop experiments |

### `internal/brief`
| File | Exports / Responsibility |
|------|--------------------------|
| `brief.go` | `Build()`, `BuildWithUpstream()`, `BuildLoopInjection()`, `Sections()` — assembles session context |
| `memory.go` | `RunMemoryPass()` — rewrites brief.md after session; `RunRefresh()` — rebuilds from journals |

### `internal/updater`
| File | Exports / Responsibility |
|------|--------------------------|
| `updater.go` | `Update()` — checks GitHub releases API, downloads + replaces binary |

## Dependency Graph (simplified)
```
main.go → cmd/
  cmd/root.go → internal/config, internal/queue
  cmd/init.go → internal/scaffold
  cmd/prompt.go → internal/brief, internal/claude
  cmd/run.go → internal/brief, internal/claude, internal/queue
  cmd/refresh.go → internal/brief
  cmd/update.go → internal/updater

  internal/config → internal/claude (RunnerOpts type)
  internal/scaffold → internal/config, internal/claude
  internal/brief → internal/claude
```
